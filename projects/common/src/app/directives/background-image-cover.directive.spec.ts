import { BackgroundImageCoverDirective } from './background-image-cover.directive';

describe('BackgroundImageCoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImageCoverDirective();
    expect(directive).toBeTruthy();
  });
});
